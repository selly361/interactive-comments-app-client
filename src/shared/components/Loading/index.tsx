import { LoadingCircle, StyledLoading } from './Loading.styles'

import React from 'react'

function Loading() {
   return (
      <StyledLoading>
         <LoadingCircle>Loading...</LoadingCircle>
      </StyledLoading>
   )
}

export default Loading
